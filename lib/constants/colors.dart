import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(0xFF6C63FF); // Nowy kolor główny (fioletowy)
  static const Color secondaryColor = Color(0xFF00BFA6); // Nowy kolor akcentu (turkusowy)
  static const Color backgroundColor = Color(0xFFF5F5F5); // Jasny szary dla tła
  static const Color textColor = Color(0xFF333333); // Ciemny szary dla tekstu
  static const Color white = Color(0xFFFFFFFF);

  // Dodatkowe kolory
  static const Color buttonColor = primaryColor;
  static const Color buttonTextColor = white;
}
